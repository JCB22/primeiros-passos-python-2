import os

# /k significa que vai dar para ver o output
os.system('cmd /k "python --version " ')

# /c significa que não dá pra ver o output
os.system('cmd /c "python --version"')

""" 
 OBS: Esse comentário foi escrito devido ao command date que
 pede para botar uma data nova

 Quando o programa pedir para botar uma data nova, só da um Enter
 que ele então não faz nada
"""